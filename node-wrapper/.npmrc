# Hard-fail installs on an unsupported Node version (package.json engines.node
# is otherwise only advisory). Keeps the musl/Node-22 floor enforced in CI and
# on contributor machines.
engine-strict=true
